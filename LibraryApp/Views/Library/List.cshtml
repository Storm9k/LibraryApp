@model LibraryApp.Models.ViewModels.BookList

@foreach (var p in Model.Books)
{
    @*<div class="well">
        <h3>
            <strong>@p.Name</strong>
            <span class="pull-right label label-primary">
                @p.Price.ToString("c")
            </span>>
        </h3>
        <span class="lead"> @p.Description</span>
        <a asp-action="Delete" asp-controller="Library" asp-route-id="@p.ID">Удалить</a>
    </div>*@
    @Html.Partial("LibrarySummary", p)
}
<div page-model="@Model.PagingInfo" page-action="List" 
        page-classes-enabled="true" 
        page-class="btn" 
        page-class-normal="btn-default" 
        page-class-selected="btn-primary"
        page-url-genre=@Model.PagingInfo.CurrentGenre
        class="btn-group pull-right">
</div>